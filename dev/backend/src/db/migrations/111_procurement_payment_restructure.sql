-- =====================================================
-- 111: 采购审批付款方式改造 (v4 → v5)
--
-- 变更内容：
-- 1. 付款方式从4选项(paymentMethod)简化为"是否需要预付款"(needPrepayment: yes/no)
-- 2. 新增预付金额字段(prepaymentAmount)，可编辑，默认=订单总额
-- 3. 流程从10节点精简为7节点：审批链→出纳付款→创建预付款→审核PO→办结
-- 4. 移除库管到货确认、多货子流程、已付款核销分支
-- 5. 总经理审批阈值从5000元调整为50000元
--
-- 兼容性：
-- - form_schema 和 workflow_def 的权威定义在代码中
--   (dev/backend/src/services/oa/form-types/procurement-order.ts)
-- - 在途审批实例不受影响（节点已快照到 oa_approval_nodes 表）
-- - 无需数据迁移，仅代码级变更
-- =====================================================

-- 无表结构变更，此为注释性迁移
